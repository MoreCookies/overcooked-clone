class Item {
    constructor(id, recipes, imgref=null) {
        this.id = id
        this.recipes=recipes
        //formatted like {"CHOP": "tomato_soup", "COMBINE": }
    }

    change(action) {
        
    }
}